# Test

## Scripts

- `init_skill.py` — scaffold a skill
- `ALL_SKILLS` is a bash array
- Use `cat_apm_core` variable

**pay-agent-skills** is a repo.

Use `python3 scripts/package_skill.py` to bundle.

``` bash
echo hello
```

``` json
{"json": true}
```
