# Corpus

Sample documentation used for integration and benchmark tests.

## Inline code

Use `init_skill.py` and `ALL_SKILLS` in examples.

**Important:** always review diffs before running `mdmend fix`.

```bash
echo hello
```